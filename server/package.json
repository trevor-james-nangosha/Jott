{
  "name": "@jott/server",
  "version": "1.0.0",
  "description": "The application server for the Jott application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc --project tsconfig.json",
    "start": "npm run migrations && npm run tsc && npx nodemon index.ts",
    "migrations": "knex migrate:latest"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.17",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.4.2",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1"
  }
}
